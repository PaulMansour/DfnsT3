'⍝ Conditional application:⍞000D⍞000D    eis←1∘=∘≡cond⊂cond⊢         ⍝ enclose if depth=1.⍞000D⍞000D    disp      1 (,1) (⊂,1)      ⍝ items have depth 0 1 2⍞000D┌─┬─┬───┐⍞000D│1│1│┌─┐│⍞000D│ │ ││1││⍞000D│ │ │└─┘│⍞000D└─┴─┴───┘⍞000D    disp eis¨ 1 (,1) (⊂,1)      ⍝ enclose depth-1 items.⍞000D┌─┬───┬───┐⍞000D│1│┌─┐│┌─┐│⍞000D│ ││1│││1││⍞000D│ │└─┘│└─┘│⍞000D└─┴───┴───┘⍞000D⍞000D    0 1 ÷cond-¨ 3               ⍝ dyadic (as opposed to triadic) cond.⍞000D¯3 0.3333333333⍞000D⍞000D    unless←{~∘⍵⍵ cond ⍺⍺ cond⊢⍵}⍞000D    log←⍟unless(≤∘0)⍞000D    log 3⍞000D1.098612289⍞000D    log 0⍞000D0⍞000D    or←{(∨/⍺)cond((0⊥⍺)cond ⍵⍵ cond((2=⍴⍺)cond ⍺⍺ cond((¯1↓⍺)∘⍺⍺)))cond⊢⍵}⍞000D    0 0 0 0-or×or⌈or⌊7.5⍞000D7.5⍞000D    0 0 0 1-or×or⌈or⌊7.5⍞000D7⍞000D    0 0 1 0-or×or⌈or⌊7.5⍞000D8⍞000D    0 1 0 0-or×or⌈or⌊7.5⍞000D1⍞000D    1 0 0 0-or×or⌈or⌊7.5⍞000D¯7.5⍞000D⍞000D⍝ try alternative coding:⍞000D⍞000D    cond←{ ⍝ proposition : consequence : alternative⍞000D        m q y←147036925 258147036 369258147⍞000D        ⍺←m⍞000D        Õ←{⊃⊢∘⍺⍺/(⍳⍵⍵),⊂⍵}⍞000D        ⊢∘⍺⍺ Õ(⍺=q)⊢⊢∘⍵⍵ Õ(⍺=y)⊢⍺⍺{(q ⍺⍺ ⍵)⍺⍺{y∘⍺⍺ Õ ⍺⊢⊢∘⍵⍵ Õ(~⍺)⊢⍵}⍵⍵ ⍵}⍵⍵ Õ(⍺=m)⊢⍵⍞000D    }⍞000D⍞000D    eis←1∘=∘≡cond⊂cond⊢         ⍝ enclose if depth=1.⍞000D⍞000D    disp eis¨ 1 (,1) (⊂,1)      ⍝ enclose depth-1 items.⍞000D┌─┬───┬───┐⍞000D│1│┌─┐│┌─┐│⍞000D│ ││1│││1││⍞000D│ │└─┘│└─┘│⍞000D└─┴───┴───┘' 
