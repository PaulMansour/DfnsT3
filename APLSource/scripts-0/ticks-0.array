'⍝ Sample Dfn execution clock ticks.⍞000D⍞000D⍝ [ticks] uses ⎕← to display monitoring information in the session.⍞000D⍝ For testing, we modify the temporary copy of the operator in this⍞000D⍝ namespace to output to variable ∆out. To save time, we also reduce⍞000D⍝ the number of ticks by a factor of 100.⍞000D⍞000D    sout←''⎕←'' ''_←{∆out∘←⍵}''∘subs        ⍝ subs ∆out← for ⎕←⍞000D    stix←''1000'' ''10''∘subs               ⍝ only 10 ticks.⍞000D⍞000D    _←⎕fx stix sout ⎕cr''ticks''          ⍝ modify ticks operator.⍞000D⍞000D    tix←{                               ⍝ test modified operator.⍞000D        ⍺←⊢                             ⍝ optional left arg.⍞000D        _←⍺ ⍺⍺ ticks ⍵                  ⍝ run op, ignore result.⍞000D        a b←2↑{⍵/⍳⍴⍵}{^⌿⍵='' ''}∆out      ⍝ first two blank cols.⍞000D        visits←↑a↑¨↓∆out                ⍝ visits and⍞000D        source←↑b↓¨↓∆out                ⍝ source columns.⍞000D        visits,''-'',''-'',source           ⍝ ignore percentage col.⍞000D    }⍞000D⍞000D    dups←{⍞000D        dup←{⍵ ⍵}⍞000D        dup¨{⍵ ⍵}¨⍵ ⍵⍞000D    }⍞000D⍞000D    dups tix 0⍞000D1 -- dups←{               ⍞000D2 --         dup←{⍵ ⍵}    ⍞000D1 --         dup¨{⍵ ⍵}¨⍵ ⍵⍞000D0 --     }                ⍞000D⍞000D    osc tix 27⍞000D112 -- osc←{               ⍝ Oscillate - probably returns 1.⍞000D112 --     1=⍵:1                                            ⍞000D111 --     2|⍵:∇ 1+3×⍵                                      ⍞000D 70 --         ∇ ⍵÷2                                        ⍞000D  0 -- }                                                    ⍞000D⍞000D    105 gcd tix 330⍞000D4 -- gcd←{       ⍝ Greatest common divisor.⍞000D4 --     ⍵=0:|⍺                            ⍞000D3 --     ⍵ ∇ ⍵|⍺                           ⍞000D0 -- }                                     ⍞000D⍞000D    105 gcd¨ ticks 300 330              ⍝ not a simple dfn.⍞000D11::bad operand' 
