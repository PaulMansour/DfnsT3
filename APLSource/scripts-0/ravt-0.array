'⍝ Generic tree traversal: visits node _after_ sub-nodes:⍞000D⍞000D    {}''t.r'' ''t.a'' ''t.v''⎕ns¨⊂''''      ⍝ make some spaces.⍞000D⍞000D    subs ← {⍵.(1↓⍎¨''#'',↓⎕nl 9)}     ⍝ sub-namespaces for space ⍵.⍞000D⍞000D    accm ← {⍺+1}                    ⍝ counting namespaces.⍞000D    0 accm ravt subs t              ⍝ number of spaces.⍞000D4⍞000D    accm ← ,                        ⍝ accumulating into a vector.⍞000D    vec ← ⍬ accm ravt subs t        ⍝ sub-nodes first:⍞000D⍞000D    vec ≡ t.a t.r t.v t             ⍝ check match.⍞000D1' 
