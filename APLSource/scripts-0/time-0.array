'⍝ Time function application:⍞000D⍞000D⍝ [time] uses ⎕← to display monitoring information in the session.⍞000D⍝ For testing, we modify the temporary copy of the operator in this⍞000D⍝ namespace to output to variable ∆out.⍞000D⍞000D    sout←''⎕←'' ''_←{∆out∘←⍵}''∘subs        ⍝ subs ∆out← for ⎕←⍞000D⍞000D    _←⎕fx sout ⎕cr''time''                ⍝ modify time operator.⍞000D⍞000D    ∆out←''''                             ⍝ initialise output buffer.⍞000D    + time 0                            ⍝ time function application.⍞000D0⍞000D    ↑∆out                               ⍝ show traced output.⍞000D00.00' 
