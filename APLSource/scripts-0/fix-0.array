'⍝ Fix function/operator rep:⍞000D⍞000D    ⎕io←1⍞000D⍞000D    ⎕fx''z←tfn n'' ''z←⍳n''             ⍝ trad fn.⍞000Dtfn⍞000D    dfn←{3↑⍵}                       ⍝ dfn.⍞000D    drv←×/                          ⍝ derived fn.⍞000D⍞000D    vec←rep¨''tfn'' ''dfn'' ''drv''⍞000D⍞000D      disp vec                      ⍝ vector of function reps.⍞000D┌──────┬──────┬──────────┐⍞000D│ ∇tfn │ ∇dfn │┌───┬────┐│⍞000D│      │      ││drv│ ×/ ││⍞000D│      │      │└───┴────┘│⍞000D└──────┴──────┴──────────┘⍞000D⍞000D      ⊢fix¨vec                      ⍝ fix functions.⍞000D tfn  dfn  drv ⍞000D⍞000D      disp ⎕cr¨''drv'' ''dfn'' ''tfn''    ⍝ function ''source''.⍞000D┌──┬─────────┬───────┐⍞000D│×/│dfn←{3↑⍵}│z←tfn n│⍞000D│  │         │z←⍳n   │⍞000D└──┴─────────┴───────┘⍞000D⍞000D      drv dfn tfn 6                 ⍝ test functions.⍞000D6' 
