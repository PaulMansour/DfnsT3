'⍝ Approx expression timings:⍞000D⍞000D⍝ This test is problematic in that timings can vary slightly depending on⍞000D⍝ machine loading.   To avoid false negatives, the results are amended to⍞000D⍝ ignore tenths of a second.⍞000D⍞000D    fudge←{v←⍕⍵ ⋄ ((¯1+v⍳''E'')⊃v)←''?'' ⋄ v}       ⍝ 1.?E¯1⍞000D⍞000D⍝ The volatile percentage differences are removed from the graphical display:⍞000D⍞000D    squish←{(~≠\∧⌿⍵∊''|%'')/⍵}                    ⍝ without percentages⍞000D⍞000D⍝ Finally, to reduce the time taken to run this test, cmpx is modified so that⍞000D⍝ it finishes after the expressions have run for just a tenth of second.⍞000D⍞000D    {}⎕fx''secs×1000'' ''secs×100''∘subs¨⎕nr''cmpx''  ⍝ reduce no of iterations.⍞000D⍞000D⍝ timing of single expression:⍞000D⍞000D    fudge cmpx''⎕dl 0.1''⍞000D1.?E¯1⍞000D⍞000D⍝ comparison of multiple expressions:⍞000D⍞000D    ↑ fudge¨ ↓ squish 0 0 10 cmpx''⎕dl 0.2'' ''⎕dl 0.4''⍞000D  ⎕dl 0.2 → 2.?E¯1 % ⎕⎕⎕⎕⎕     ⍞000D* ⎕dl 0.4 → 4.?E¯1 % ⎕⎕⎕⎕⎕⎕⎕⎕⎕⎕⍞000D' 
