'⍝ Simple macro processor for bf:⍞000D⍞000D    movl←''l=[-/<+/>] ''      ⍝ move left ⍺ cell positions ("/" replicates.).⍞000D⍞000D    mac movl, ''2l 1l 0l''    ⍝ move left 2 1 0 (the last of which will hang BF).⍞000D[-<<+>>] [-<+>] [-+]⍞000D⍞000D    mac''a=KO a=OK a''        ⍝ subsequent defns replace previous ones.⍞000DOK⍞000D    mac''a=K (a=O a)a''       ⍝ inner block defns shadow outer ones.⍞000DOK⍞000D    mac''a=OK b=a a=KO b''    ⍝ a expanded at b defn time.⍞000DOK⍞000D    mac''a=KO b=\a a=OK b''   ⍝ a expanded at b dref time.⍞000DOK⍞000D    mac''O=? b=\\\OK b''      ⍝ O not expanded.⍞000DOK⍞000D    mac''a=K b=(a=O a) ba''   ⍝ inner defn is local to block.⍞000DOK' 
