'⍝ Put rows to text-file:⍞000D⍞000D    fid←''Dfns Test Temp File''       ⍝ temp file for testing.⍞000D⍞000D    vals←1↓,\15↓⎕av                 ⍝ vector of char vectors.⍞000D⍞000D    ctype←⎕dr 15↓⎕av                ⍝ character type.⍞000D⍞000D    size ← putfile fid vals         ⍝ put to temp file.⍞000D⍞000D    vals≡0 ctype getfile fid        ⍝ get from temp file.⍞000D1⍞000D    size ← 1 putfile fid vals       ⍝ nl-terminated lines.⍞000D⍞000D    vals≡0 ctype getfile fid        ⍝ get from temp file.⍞000D1⍞000D    size ← 0 putfile fid vals       ⍝ non-terminated lines.⍞000D⍞000D    (↑,/vals)≡⊃0 ctype getfile fid  ⍝ get from temp file.⍞000D1⍞000D    {}fid ⎕nerase fid ⎕ntie 0       ⍝ remove temp file.' 
