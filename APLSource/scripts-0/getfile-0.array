'⍝ Ascii file as nested vector:⍞000D⍞000D    fid←''Dfns Test Temp File''⍞000D⍞000D    zap←{22::0 ⋄ {1}⍵ ⎕nerase ⍵ ⎕ntie 0}        ⍝ remove file ⍵.⍞000D⍞000D    zap fid                                     ⍝ ensure file doesn''t exist.⍞000D0⍞000D    getfile fid                                 ⍝ test bad name trap.⍞000D22::FILE NAME ERROR⍞000D⍞000D    cr lf ht←⎕ucs 13 10 9                       ⍝ "terminal control" chars.⍞000D⍞000D    cvec←''''                                     ⍝ write-buffer.⍞000D    ⍞000D    cvec,← ''it''''s'',                    cr, lf⍞000D    cvec,← ''spring'',                   cr, lf⍞000D    cvec,← ''and'',                      cr, lf⍞000D    cvec,← ht,''the'',                   cr, lf⍞000D    cvec,←                             cr, lf⍞000D    cvec,← ht,ht,''goat-footed'',        cr, lf⍞000D    cvec,←                             cr, lf⍞000D    cvec,← ''balloonMan'',ht,''whistles'',     lf   ⍝ getfile is permissive⍞000D    cvec,← ''far'',                          lf   ⍝ about missing cr.⍞000D    cvec,← ''and'',                      cr, lf⍞000D    cvec,← ''wee'',                      cr, lf⍞000D    cvec,← ht,''... e.e.cummings''                ⍝ missing final lf is OK.⍞000D⍞000D    ctype←⎕dr cvec                              ⍝ character type.⍞000D⍞000D    ntie←fid ⎕ncreate 0                         ⍝ create new file.⍞000D    _←cvec ⎕nappend ntie ctype                  ⍝ append line to file.⍞000D    _←⎕nuntie ntie                              ⍝ release file.⍞000D⍞000D    st ← ht ''→'' ∘ subs ¨                        ⍝ show tabs as ''→''s.⍞000D⍞000D    disp st 0 ctype getfile fid                 ⍝ preserve tab chars.⍞000D┌────┬──────┬───┬────┬┬─────────────┬┬───────────────────┬───┬───┬───┬─────────────────┐⍞000D│it''s│spring│and│→the││→→goat-footed││balloonMan→whistles│far│and│wee│→... e.e.cummings│⍞000D└────┴──────┴───┴────┴┴─────────────┴┴───────────────────┴───┴───┴───┴─────────────────┘⍞000D⍞000D    disp st 4 ctype getfile fid                 ⍝ expand 4-tabs.⍞000D┌────┬──────┬───┬───────┬┬───────────────────┬┬────────────────────┬───┬───┬───┬────────────────────┐⍞000D│it''s│spring│and│    the││        goat-footed││balloonMan  whistles│far│and│wee│    ... e.e.cummings│⍞000D└────┴──────┴───┴───────┴┴───────────────────┴┴────────────────────┴───┴───┴───┴────────────────────┘⍞000D⍞000D    ↑ 4 getfile fid                             ⍝ ... and mix to char matrix.⍞000Dit''s                ⍞000Dspring              ⍞000Dand                 ⍞000D    the             ⍞000D                    ⍞000D        goat-footed ⍞000D                    ⍞000DballoonMan  whistles⍞000Dfar                 ⍞000Dand                 ⍞000Dwee                 ⍞000D    ... e.e.cummings⍞000D⍞000D    zap fid                                     ⍝ remove test file⍞000D1' 
