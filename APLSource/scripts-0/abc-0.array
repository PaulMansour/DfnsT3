'⍝ ABC - Arithmetic Boundary Checking:⍞000D⍞000D    ⎕io ⎕ml←0 3                 ⍝ Environment for tests.⍞000D⍞000D    bp←{⊃(⍺<⍵)(⍺>⍵)}            ⍝ Boolean pair (2-vector).⍞000D⍞000D    2 3 4 bp 3                  ⍝ ⍺<⍵←→1 0 ⋄ ⍺=⍵←→0 0 ⋄ ⍺>⍵←→0 1.⍞000D1 0 0⍞000D0 0 1⍞000D⍞000D    xd←{×⍺-⍵}                   ⍝ Signum difference.⍞000D⍞000D    2 3 4 xd 3                  ⍝ ⍺<⍵←→¯1  ⋄ ⍺=⍵←→0  ⋄ ⍺>⍵←→1.⍞000D¯1 0 1⍞000D    bd←{(⍺>⍵)-(⍺<⍵)}            ⍝ Boolean difference (same as xd).⍞000D⍞000D    2 3 4 bd 3                  ⍝ ⍺<⍵←→¯1  ⋄ ⍺=⍵←→0  ⋄ ⍺>⍵←→1.⍞000D¯1 0 1⍞000D⍞000D    rg←{(⍺[0]⍺⍺ ⍵)∧⍺[1]⍵⍵ ⍵}    ⍝ Range Operator.⍞000D⍞000D    1 3 <rg> ⍳5                 ⍝ Open Interval:        ○-----------○⍞000D0 0 1 0 0⍞000D⍞000D    1 3 ≤rg≥ ⍳5                 ⍝ Closed Interval:      ⍟-----------⍟⍞000D0 1 1 1 0⍞000D⍞000D    1 3 <rg≥ ⍳5                 ⍝ Half-Open Left:       ○-----------⍟⍞000D0 0 1 1 0⍞000D⍞000D    1 3 ≤rg> ⍳5                 ⍝ Half-Open Right:      ⍟-----------○⍞000D0 1 1 0 0⍞000D⍞000D    xp←{×/×⍵∘.-⍺}               ⍝ Signum product.⍞000D⍞000D    1 3 xp ⍳5                   ⍝ produces interval partition.⍞000D1 0 ¯1 0 1⍞000D⍞000D    xs←{+/×⍵∘.-⍺}               ⍝ Signum sum.⍞000D⍞000D    1 3 xs ⍳5                   ⍝ produces range partition.⍞000D¯2 ¯1 0 1 2⍞000D⍞000D    xm←{⌈/⊃×⌿×⍺,.-⍉⍵}           ⍝ Max Signum.⍞000D⍞000D    b←⊃(3 2)(5 6)               ⍝ Upper Left and Lower Right Corners.⍞000D⍞000D    p←(6 8)(5 2)(4 4)(7 6)(3 5)(2 3)            ⍝ Various Points in the plane.⍞000D⍞000D    b xm ⊃p                     ⍝ 1=Outside, 0=Boundary, ¯1=Interior.⍞000D1 0 ¯1 1 0 1⍞000D⍞000D    xr←{d←⍉⊃+/×⍵,.-⍉⍺ ⋄ ((2∨.=|d)/d)←2 ⋄ 3⊥d}   ⍝ Signum region.⍞000D⍞000D    b xr ⊃p                     ⍝ 0=Interior, 8=Outside, Else boundary.⍞000D8 2 0 8 ¯3 8' 
