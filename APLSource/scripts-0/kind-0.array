'⍝ parse-category of name(s) ⍵.⍞000D⍞000D    var←''var''⍞000D    fun←{⍵ ⍵}⍞000D    mop←{⍺⍺ ⍵}⍞000D    dop←{⍵⍵ ⍵}    ⍞000D    ⍞000D    (kind,⊢) ↑''und'' ''var'' ''fun'' ''mop'' ''dop''⍞000D0 und⍞000D1 var⍞000D2 fun⍞000D3 mop⍞000D4 dop⍞000D⍞000D    kind ''##.scripts''       ⍝ namespace parses as var⍞000D1⍞000D    kind ⎕fx''foo'' ''''        ⍝ niladic fn parses as var⍞000D1⍞000D    mop←¨ ⋄ fun←+ ⋄ hyb←⌿   ⍝ primitive mop, fun and hybrid⍞000D    kind ''fun'' ''hyb'' ''mop''⍞000D2 2.5 3⍞000D' 
