'indx ← notespace ##.index class             ⍝ ⍵-index of notes in space ⍺.⍞000D⍞000D[index]  extracts  lines starting ''Index'',[class] from character arrays in space⍞000D[notespace]  and  returns a vector of (entry refs) pairs:⍞000D⍞000D    entry:  index entry (character vector)⍞000D    refs:   names of variables referencing entry (vector of character vectors).⍞000D⍞000DMultiple entries on the same line are separated by character ''|''.⍞000D⍞000DThe  notes  in this workspace use the convention '':'' to add entries to a ≤topic≥⍞000Dindex,  '';''  to  add  entries  to a ≤person≥ index, and ''≡'' to add entries to an⍞000Dalias list.⍞000D⍞000D    Index:  topic_entry|topic_entry|...⍞000D    Index;  person_entry|...⍞000D    Index≡  alias, for example "network, see: graph", as follows:⍞000D⍞000DAliases:⍞000D⍞000DIndex≡BFS→breadth-first search|network→graph|recursion→recursion|Phi→golden mean⍞000DIndex≡balanced tree→binary search tree|IDN→International Day Number⍞000DIndex≡AGM→arithmetic-geometric mean|BST→binary search tree⍞000DIndex≡DFS→depth-first search|MST→minimum spanning tree|RLE→run-length encoding⍞000DIndex≡line list→line vector|FRE→Function Results Edition|LZW→Lempel-Ziv-Welch⍞000DIndex≡spaces→blanks|GCD→greatest common divisor|LCM→least common multiple⍞000DIndex≡JITS→Just-In-Time-Subtraction|HCF→greatest common divisor⍞000DIndex≡AST→abstract syntax tree|CPS→continuation-passing style|Ø→golden mean⍞000DIndex≡imaginary numbers→complex numbers|HHKB→Happy Hacking Keyboard⍞000DIndex≡line-drawing characters→box-drawing characters|FFT→Fast Fourier Transform⍞000DIndex≡data compression→compression|TAO→Total array ordering⍞000DIndex≡read-eval-print loop→repl|nested array→array, nested|EIS→enclose-if-simple⍞000D⍞000DExample:⍞000D⍞000D    ↑ 5↑ notes index '':''                ⍝ first 5 _topic_ index entries.⍞000D ⍺th root      root⍞000D ⍺←            root⍞000D accumulator   factorial  fibonacci  list⍞000D adjacent 1s   xtabs⍞000D AGM           limit⍞000D⍞000D    ↑ 5↑ notes index '';''                ⍝ first 5 _person_ index entries.⍞000D Adelson-Velskii G.M.   avl⍞000D Baas M.                pred⍞000D Baronet D.             dtb⍞000D Beckham D.             mayan⍞000D Bopp Y.                enlist⍞000D⍞000D    ↑ 5↑ notes index '':;''               ⍝ first 5 _combined_ index entries.⍞000D ⍺th root               root⍞000D ⍺←                     root⍞000D accumulator            factorial  fibonacci  list⍞000D Adelson-Velskii G.M.   avl⍞000D adjacent 1s            xtabs⍞000D⍞000D    {''see '',⍵}\↑{1↓¨(''→''=⍵)⊂⍵}∘{''→'',⊃⍵}¨notes index''≡''      ⍝ alias entries⍞000D AGM            see arithmetic-geometric mean⍞000D AST            see abstract syntax tree⍞000D balanced tree  see binary search tree⍞000D BFS            see breadth-first search⍞000D BST            see binary search tree⍞000D DFS            see depth-first search⍞000D FRE            see Function Results Edition⍞000D GCD            see greatest common divisor⍞000D HCF            see greatest common divisor⍞000D IDN            see International Day Number⍞000D JITS           see Just-In-Time-Subtraction⍞000D LCM            see least common multiple⍞000D line list      see line vector⍞000D LZW            see Lempel-Ziv-Welch⍞000D MST            see minimum spanning tree⍞000D network        see graph⍞000D Phi            see golden mean⍞000D recursion      see recursion⍞000D RLE            see run-length encoding⍞000D spaces         see blanks⍞000D⍞000DSee also: xhtml' 
