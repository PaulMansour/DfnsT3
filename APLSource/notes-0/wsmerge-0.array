'{protected←0} ##.wsmerge wsid          ⍝ Merge saved ws into current active ws.⍞000D⍞000D[wsmerge]  merges  the saved workspace [wsid] into the current active workspace.⍞000DThe  optional  left argument (default 0) determines whether incoming objects are⍞000Dprevented  from  overwriting  existing  ones of the same name. [wsmerge] differs⍞000Dfrom  )COPY  and )PCOPY in that it merges, rather than replaces, namespaces with⍞000Dincoming ones of the same name.⍞000D⍞000DTechnical note:⍞000D⍞000DTo  deliver a vector of namespace references, [wsmerge] incorporates rather than⍞000Dcalls,  subfunction  →refs←.  This  means that it can be copied as a stand-alone⍞000Dutility, into an active workspace. See →refs← for details.⍞000D⍞000DExamples:⍞000D⍞000D⍝───────────────────────────────────────────────────────────────────────────────⍞000D⍞000D      )load aaa                     ⍝ load WS "aaa".⍞000D··· saved ···⍞000D⍞000D      tree #                        ⍝ show aaa''s content.⍞000D#⍞000D·   ~ aaa ccc⍞000D·   notes⍞000D·   ·   ~ aaa⍞000D⍞000D      ccc                           ⍝ show aaa''s variable "ccc".⍞000Daaa''s ccc⍞000D⍞000D⍝───────────────────────────────────────────────────────────────────────────────⍞000D⍞000D      )load bbb                     ⍝ load WS "bbb".⍞000D··· saved ···⍞000D⍞000D      tree #                        ⍝ show bbb''s content.⍞000D#⍞000D·   ~ bbb ccc⍞000D·   notes⍞000D·   ·   ~ bbb⍞000D⍞000D      ccc                           ⍝ show bbb''s variable "ccc".⍞000Dbbb''s ccc⍞000D⍞000D⍝───────────────────────────────────────────────────────────────────────────────⍞000D⍞000D      )load aaa                     ⍝ load WS "aaa".⍞000D··· saved ···⍞000D⍞000D      )copy dfns wsmerge            ⍝ copy wsmerge into "aaa".⍞000D··· saved ···⍞000D⍞000D      1 wsmerge ''bbb''               ⍝ PROTECTED merge "bbb".⍞000D⍞000D⍝ Note how content of aaa''s and bbb''s namespace "notes" has been merged.⍞000D⍞000D      tree #                        ⍝ show merged content.⍞000D#⍞000D·   ~ aaa bbb ccc⍞000D·   ∇ wsmerge⍞000D·   notes⍞000D·   ·   ~ aaa bbb⍞000D⍞000D      ccc                           ⍝ aaa''s variable was protected.⍞000Daaa''s ccc⍞000D⍞000D⍝───────────────────────────────────────────────────────────────────────────────⍞000D⍞000D      )load aaa                     ⍝ reload WS "aaa".⍞000D··· saved ···⍞000D⍞000D      )copy dfns wsmerge            ⍝ copy wsmerge into "aaa".⍞000D··· saved ···⍞000D⍞000D      0 wsmerge ''bbb''               ⍝ UNPROTECTED merge "bbb".⍞000D⍞000D      tree #                        ⍝ show merged content.⍞000D#⍞000D·   ~ aaa bbb ccc⍞000D·   ∇ wsmerge⍞000D·   notes⍞000D·   ·   ~ aaa bbb⍞000D⍞000D      ccc                           ⍝ aaa''s variable was overwritten.⍞000Dbbb''s ccc⍞000D⍞000D⍝───────────────────────────────────────────────────────────────────────────────⍞000D⍞000DSee also: refs tree wsdiff⍞000D⍞000DIndex:workspace|merge, workspace' 
