'{rslt} ← {larg} (fun ##.time) rarg          ⍝ Time function application.⍞000D⍞000D[time] displays the elapsed time taken, to the nearest centi-second, when funct-⍞000Dion  [fun] is applied to or between its argument(s). The result of the applicat-⍞000Dion is returned as a shy result.⍞000D⍞000DExamples:⍞000D⍞000D      queens time 8     ⍝ 8-queens problem takes just over a tenth of a second.⍞000D00.12⍞000D⍞000D      queens time 13    ⍝ 13-queens problem takes three and a half minutes.⍞000D03:32.27⍞000D⍞000D      ''myfile''findfile time''string''     ⍝ timing of dyadic function.⍞000D00.07⍞000D⍞000D      {+/,⍵×assign      ⍵} costs        ⍝ complex expression.⍞000D223⍞000D      {+/,⍵×assign time ⍵} costs        ⍝ time displayed as side-effect.⍞000D05.57⍞000D223⍞000D⍞000DSee also: cmpx mdf ticks⍞000D⍞000DIndex:performance|monitor|timing' 
