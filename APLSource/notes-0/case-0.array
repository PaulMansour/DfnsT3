'rslt ← i j k ··· F ##.case G case H ··· arg     ⍝ Select statement.⍞000D⍞000Di j k ···  is  a  simple  boolean  vector  whose length is the number of monadic⍞000Dfunctions  (F G H ···)  in  the  derived  function.  [case] applies the function⍞000Dselected by the boolean left argument to the derived function''s right argument.⍞000D⍞000DThe statement is equivalent to:⍞000D⍞000D    :Select 1⍞000D    :Case i ⋄ rslt←F arg⍞000D    :Case j ⋄ rslt←G arg⍞000D    :Case k ⋄ rslt←H arg⍞000D    :Case l ⋄ rslt←I arg⍞000D    ...⍞000D    :Else ⋄ rslt←arg⍞000D    :End⍞000D⍞000DThe  simple alternating pattern of ... fn op fn op fn ... in the statement hides⍞000Dthe fact that the functions are at different "depths" in the derived function.⍞000D⍞000DFor  illustration  the  derived fn (f05) below has two operands to the "topmost"⍞000Dcompose operator; the right being (÷) and the left, all the rest (⌈∘⌊∘-∘×)⍞000D⍞000D    f05←⌈∘⌊∘-∘×∘÷ ⋄ ⎕cr''f05''⍞000D┌───────────────┬─┬─┐⍞000D│┌─────────┬─┬─┐│∘│÷│⍞000D││┌───┬─┬─┐│∘│×││ │ │⍞000D│││⌈∘⌊│∘│-││ │ ││ │ │⍞000D││└───┴─┴─┘│ │ ││ │ │⍞000D│└─────────┴─┴─┘│ │ │⍞000D└───────────────┴─┴─┘⍞000D⍞000DSimilarly  in  fns  derived  with (case), only (⍵⍵) is necessarily available for⍞000Dimmediate  use  so we run (⍵⍵) if the last of (⍺) is set. If not, and if (⍺) has⍞000Dlength  2 and the first is one we can run (⍺⍺) which must be at the "top" level.⍞000Dcf.  (⌈∘⌊) in (f05) above. If there are more than two items in (⍺), (⍺⍺) must be⍞000Dderived with another (case) call so we run it dyadically losing the last (⍺).⍞000D⍞000DA no-guard one-liner using conditional monad-reduction (see →pow←) is possible.⍞000D⍞000D    case←{⊃⍺⍺{b ⍺⍺ ⍵}/(i~∨/b←¯1↓b),⍺⍺{⍺⍺ ⍵}/(⍳i←1 0≡b),⍵⍵{⍺⍺ ⍵}/(⍳¯1↑b←<\⍺),⊂⍵}⍞000D⍞000DExamples:⍞000D⍞000D      1 0 0 0 + case - case × case ÷ 10      ⍝ +10⍞000D10⍞000D      0 1 0 0 + case - case × case ÷ 10      ⍝ -10⍞000D¯10⍞000D      0 0 1 0 + case - case × case ÷ 10      ⍝ ×10⍞000D1⍞000D      0 0 0 1 + case - case × case ÷ 10      ⍝ ÷10⍞000D0.1⍞000D      0 0 0 0 + case - case × case ÷ 10      ⍝  10 (no case selected).⍞000D10⍞000D⍞000DSee also: for of Function_arrays pow co_ops⍞000D⍞000DIndex:conditional operator|select operator|co-operator' 
