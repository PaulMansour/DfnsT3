'ref ← ##.mns defn                    ⍝ Make Namespace from association list ⍵.⍞000D⍞000D[defn]  is  a "name association list" of the form (''name'' class value), ···. The⍞000Dresult is a reference to a new namespace in which each name in the list has been⍞000Dinstantiated with its given class. The list may contain nested items with class=⍞000D9 to represent sub-spaces. The [mns] function was suggested by Paul Mansour.⍞000D⍞000DTechnical note:⍞000D⍞000DAs  [mns]  is a utility function, it should be able to operate in an environment⍞000Dwith  any  migration  level  and  either index origin. However, the code may not⍞000Dlocalise  and specify ⎕ML or ⎕IO as these values would be inherited by the name-⍞000Dspaces it created. For this reason, the code is written in an origin and migrat-⍞000Dion level independent way.⍞000D⍞000DExamples:⍞000D⍞000D    alist                                   ⍝ association list⍞000D┌─────────────┬────────────────────────────────────────────────────┬─────────────────────┐⍞000D│┌───┬─┬─────┐│┌───┬─┬────────────────────────────────────────────┐│┌────┬─┬────────────┐│⍞000D││mat│2│1 2 3│││sub│9│┌───────────────────┬──────────────────────┐│││spin│3│┌──────────┐││⍞000D││   │ │4 5 6│││   │ ││┌───┬─┬───────────┐│┌────┬─┬─────────────┐││││    │ ││spin←{⍉¨⍵}│││⍞000D│└───┴─┴─────┘││   │ │││dup│3│┌─────────┐│││oper│4│┌───────────┐│││││    │ │└──────────┘││⍞000D│             ││   │ │││   │ ││dup←{⍵ ⍵}││││    │ ││oper←{⍺⍺ ⍵}│││││└────┴─┴────────────┘│⍞000D│             ││   │ │││   │ │└─────────┘│││    │ │└───────────┘││││                     │⍞000D│             ││   │ ││└───┴─┴───────────┘│└────┴─┴─────────────┘│││                     │⍞000D│             ││   │ │└───────────────────┴──────────────────────┘││                     │⍞000D│             │└───┴─┴────────────────────────────────────────────┘│                     │⍞000D└─────────────┴────────────────────────────────────────────────────┴─────────────────────┘⍞000D⍞000D    tree mns alist                          ⍝ show fixed ns.⍞000D#.[Namespace]⍞000D·   ~ mat⍞000D·   ∇ spin⍞000D·   sub⍞000D·   ·   ∇ dup⍞000D·   ·   ∘ oper⍞000D⍞000D    (mns alist).(spin sub.(dup oper) mat)   ⍝ check it out.⍞000D┌───┬───┐⍞000D│1 4│1 4│⍞000D│2 5│2 5│⍞000D│3 6│3 6│⍞000D└───┴───┘⍞000D⍞000DSee also: rep⍞000D⍞000DIndex:namespace|association list|⎕ML⍞000DIndex;Mansour P.' 
