'rslt ← i j k··· F ##.for G for H··· arg  ⍝ Multiple selection of function list.⍞000D⍞000DThis  operator  can be viewed as a function list version of (pow) or it could be⍞000Dseen as a multiple choice version of (case).⍞000D⍞000DIf⍞000D    fourfns←F for G for H for K⍞000Dthen⍞000D    2 1 0 2 fourfns arg⍞000Dis⍞000D    F F G K K arg⍞000D⍞000DThe integer list left argument is anchored such that its right item controls the⍞000Dnumber of times the rightmost function is run and counts leftwards for the rest.⍞000D⍞000DThe function operands are monadic and must be capable of acting on the result of⍞000Dwhichever  of the previous (to the right) functions happen to have run. Right to⍞000Dleft operation is the order of the day.⍞000D⍞000DExamples:⍞000D⍞000D      embrace←{''('',⍵,'')''} for {''['',⍵,'']''} for {''{'',⍵,''}''} for {''<'',⍵,''>''}⍞000D⍞000D      1 0 2 3 embrace ''this''⍞000D({{<<<this>>>}})⍞000D⍞000DSee also: case pow co_ops⍞000D⍞000DIndex:conditional operator|function selection|function array|co-operator' 
