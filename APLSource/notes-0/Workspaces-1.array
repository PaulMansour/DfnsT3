'──────────⍞000DWorkspaces⍞000D──────────⍞000DFunctions  that operate on saved workspaces typically copy them into a temporary⍞000Dnamespace  for  examination. Currently, ⎕CY doesn''t return a result; a behaviour⍞000Dwhich  is problematic for D-function coding. One way around this is to embed the⍞000Dcopy in a "diamondised" execute string, the first segment of which does the copy⍞000Dand the second, a trivial expression that returns an ignored result.⍞000D⍞000D    _←⍎''tmp.⎕cy wsid ⋄ 0''⍞000D⍞000DSee also: fndiff wsdiff refws do⍞000D⍞000DIndex:workspace' 
