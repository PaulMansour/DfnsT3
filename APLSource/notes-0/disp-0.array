'cmat ← {dec←0 {cen←0}} ##.disp array        ⍝ Boxed sketch of nested array.⍞000D⍞000DA compact alternative to the →display← function for nested  arrays.  By default,⍞000Dsubarrays are shown in plain boxes.  With optional left argument [dec] set to 1,⍞000Dthe shape of the containing array is shown in its top-left corner  and  for each⍞000Dsubarray, item type is shown lower-left and shape is shown lower-right.⍞000D⍞000DNB: The default display of nested arrays can be boxed by default, using user-⍞000D    command: ]boxing.⍞000D⍞000D      ⍳2 2      ⍝ unboxed display⍞000D 1 1  1 2⍞000D 2 1  2 2⍞000D⍞000D    ]boxing ON⍞000DWas OFF⍞000D⍞000D      ⍳2 2      ⍝ boxed output⍞000D┌───┬───┐⍞000D│1 1│1 2│⍞000D├───┼───┤⍞000D│2 1│2 2│⍞000D└───┴───┘⍞000D⍞000DThe optional left argument of 1 or 2 boolean flags (default 0 0) controls:⍞000D⍞000D    ┌───── dec: type and shape decoration:  decorated (otherwise, plain)⍞000D    │ ┌─── cen: items within boxes aligned: centred   (otherwise, top-left)⍞000D    │ │⍞000D    0 0 disp ..⍞000D⍞000DWith decoration turned off, the function can be used to format simple tables for⍞000Ddisplay in the session; see example below.⍞000D⍞000DWith decoration on, _all_ of the information provided by →display←  is  present,⍞000Dexcept for  the case of a single digit number, whose type (~) may be obscured by⍞000Dits shape (─/→) decoration.⍞000D⍞000D    1 disp 8 (,8) 88 (,88)      ⍝ type (~) obscured in first two cells.⍞000D┌→┬─┬──┬──┐⍞000D│8│8│88│88│⍞000D└─┴→┴~─┴~→┘⍞000D⍞000DExamples:⍞000D⍞000D    ditty ← 2 2⍴''Tea''(2 1⍴4 2)''&''(2 40)         ⍝ nested array⍞000D⍞000D    vs←{(⍺⍺ ⍵)(⍵⍵ ⍵)}                           ⍝ comparison operator⍞000D⍞000D    disp vs display ditty                       ⍝ comparison disp vs display⍞000D ┌───┬────┐  ┌→─────────────┐⍞000D │Tea│4   │  ↓ ┌→──┐ ┌→┐    │⍞000D │   │2   │  │ │Tea│ ↓4│    │⍞000D ├───┼────┤  │ └───┘ │2│    │⍞000D │&  │2 40│  │       └~┘    │⍞000D └───┴────┘  │       ┌→───┐ │⍞000D             │ &     │2 40│ │⍞000D             │ -     └~───┘ │⍞000D             └∊─────────────┘⍞000D⍞000D      1∘disp vs display ditty                   ⍝ ... with decoration⍞000D ┌→──┬────┐  ┌→─────────────┐⍞000D ↓Tea│4   │  ↓ ┌→──┐ ┌→┐    │⍞000D │   │2   ↓  │ │Tea│ ↓4│    │⍞000D ├──→┼~──→┤  │ └───┘ │2│    │⍞000D │&  │2 40│  │       └~┘    │⍞000D └───┴~──→┘  │       ┌→───┐ │⍞000D             │ &     │2 40│ │⍞000D             │ -     └~───┘ │⍞000D             └∊─────────────┘⍞000D⍞000D⍞000D    +/∘∊¨ ∊∘''→↓~''¨ 1∘disp vs display ditty      ⍝ same no of →↓~ present in both⍞000D8 8⍞000D⍞000D      1 disp array                              ⍝ [disp] sketch of array.⍞000D┌→──┬─────────┬─────────┬────────┐⍞000D↓3.1│ Vector  │ Matrix  ↓ Array  ⍒⍞000D├~──┼────────→┼────────→┼───────→┤⍞000D│┌─┐│┌→──┬───┐│┌→──┬───┐│┌→─────┐│⍞000D││0│││Q 8│K 9││⌽0 0│0 0││⍒ #  # ││⍞000D│└⊖┘│└+─→┴+─→┘│└~─→┴~─→┘⌽└#────→┘⍒⍞000D└───┴────────→┴────────→┴───────→┘⍞000D⍞000D      display array                             ⍝ Compare with full [display].⍞000D┌→──────────────────────────────────────────────────────┐⍞000D↓         ┌→─────┐        ┌→─────┐        ┌┌→────┐      │⍞000D│ 3.1     │Vector│        ↓Matrix│        ↓↓Array│      │⍞000D│         └──────┘        └──────┘        └└─────┘      │⍞000D│ ┌─────┐ ┌→────────────┐ ┌→────────────┐ ┌┌→─────────┐ │⍞000D│ │ ┌⊖┐ │ │ ┌→──┐ ┌→──┐ │ ⌽ ┌→──┐ ┌→──┐ │ ↓↓ ┌→─────┐ │ │⍞000D│ │ │0│ │ │ │Q 8│ │K 9│ │ │ │0 0│ │0 0│ │ ││ │ #  # │ │ │⍞000D│ │ └~┘ │ │ └+──┘ └+──┘ │ │ └~──┘ └~──┘ │ ││ └#─────┘ │ │⍞000D│ └∊────┘ └∊────────────┘ └∊────────────┘ └└∊─────────┘ │⍞000D└∊──────────────────────────────────────────────────────┘⍞000D⍞000D⍝ Simple table in session:⍞000D⍞000D      disp''%'' ''Eye Poke'' ''Kumquat''⍪↑(''Guys'' 60 40)(''Gals'' 20 80)⍞000D┌────┬────────┬───────┐⍞000D│ %  │Eye Poke│Kumquat│⍞000D├────┼────────┼───────┤⍞000D│Guys│   60   │  40   │⍞000D├────┼────────┼───────┤⍞000D│Gals│   20   │  80   │⍞000D└────┴────────┴───────┘⍞000D⍞000D⍝ The following example shows the effect of permutations of left-argument⍞000D⍝ options:⍞000D⍝⍞000D⍝   dec: type and shape border decorations off/on,⍞000D⍝       cen: centre-alignment within box off/on.⍞000D⍞000D    opts ← ⎕io<⍳2 2                 ⍝ all option permutations⍞000D⍞000D    labs ← ''decorated, '' ''centred''  ⍝ option labels with:⍞000D⍞000D    un←{∊((~⍺)/¨⊂''un''),¨⍵}          ⍝ un- prefix where appropriate⍞000D⍞000D    stuff ← '''' ''like this''(3 0⍴'''')  ⍝ content of all but top left cell⍞000D⍞000D    opts {⍺ disp 2 2⍴(⊂⍺ un ⍵),stuff}¨ ⊂labs⍞000D ┌──────────────────────┬┐  ┌────────────────────┬┐⍞000D │undecorated, uncentred││  │undecorated, centred││⍞000D ├──────────────────────┼┤  ├────────────────────┼┤⍞000D │like this             ││  │                    ││⍞000D │                      ││  │     like this      ││⍞000D │                      ││  │                    ││⍞000D └──────────────────────┴┘  └────────────────────┴┘⍞000D ┌→───────────────────┬─┐   ┌→─────────────────┬─┐⍞000D ↓decorated, uncentred│ │   ↓decorated, centred│ │⍞000D ├───────────────────→┼⊖┤   ├─────────────────→┼⊖┤⍞000D │like this           │ │   │                  │ │⍞000D │                    │ │   │    like this     │ │⍞000D │                    │ ↓   │                  │ ↓⍞000D └───────────────────→┴⊖┘   └─────────────────→┴⊖┘⍞000D⍞000DSee also: display dsp box⍞000D⍞000DIndex:display (of arrays)|box|boxing|Kumquat|box-drawing characters|array, nested' 
