'tree ← wgraph ##.wspan from        ⍝ Spanning tree for weighted graph ⍺ from ⍵.⍞000D⍞000D[wspan]  constructs  a  tree of the lowest-cost paths from the given root to all⍞000Daccessible vertices of the weighted  graph.⍞000D⍞000DEach item of the vector result, with the exception of the root item (¯1), is the⍞000Dindex of the corresponding tree node''s parent node.⍞000D⍞000DTechnical notes:⍞000D⍞000DThis coding differs slightly from classic breadth-first search, see →bfs←.⍞000D⍞000DExamples:⍞000D⍞000D    aa                  ⍝ simple weighted graph.⍞000D┌───┬─┬───┬───┬─┐⍞000D│2 3│3│2 4│1 5│3│⍞000D├───┼─┼───┼───┼─┤⍞000D│1 3│1│4 1│1 1│1│⍞000D└───┴─┴───┴───┴─┘⍞000D⍞000D    aa wspan 1          ⍝ spanning tree for aa from vertex 1.⍞000D¯1 1 2 3 4⍞000D⍞000DSee also: wGraphs Graphs⍞000DSee also: bfs wpath wcost stpath span⍞000D⍞000DIndex:graph, weighted|spanning tree' 
