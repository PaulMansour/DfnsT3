'refs ← ##.xrefs array                       ⍝ Extract refs vector from array ⍵.⍞000D⍞000DReturns a vector of unique namespace references extracted from the  array  argu-⍞000Dment.⍞000D⍞000DExamples:⍞000D⍞000D    )ns x⍞000D    )ns y⍞000D⍞000D    ⊢aa←y((1 2)x)y              ⍝ nested array containing refs.⍞000D┌───┬─────────┬───┐⍞000D│#.y│┌───┬───┐│#.y│⍞000D│   ││1 2│#.x││   │⍞000D│   │└───┴───┘│   │⍞000D└───┴─────────┴───┘⍞000D⍞000D    xrefs aa                    ⍝ unique refs.⍞000D #.y  #.x⍞000D⍞000D⍝ The  following expression returns a vector of references to all of the name-⍞000D⍝ spaces in the workspace. It includes those enclosed in any variables and any⍞000D⍝ bound by a derived function, such as: kwd←notes∘find.⍞000D⍝⍞000D⍝     ┌───────────────────────────────────────────────── Unique values of ...⍞000D⍝     │   ┌───────────────────────────────────────────── all refs together with⍞000D⍝     │   │ ┌─────────────────────────────────────────── extracted refs from⍞000D⍝     │   │ │         ┌───────────────────────────────── values of⍞000D⍝     │   │ │         │      ┌────────────────────────── all variables and⍞000D⍝     │   │ │         │      │       ┌────────────────── all functions in⍞000D⍝     │   │ │         │      │       │           ┌────── all spaces.⍞000D⍝     │   │ │         │      │       │           │⍞000D      nub{⍵,xrefs ⍵.((⍎¨''0'',↓⎕NL 2))(⎕cr¨↓⎕nl 3)}refs #⍞000D #  #.notes⍞000D⍞000DSee also: refs vwise⍞000D⍞000DIndex:namespace references' 
