'num ← ##.mean nvec                          ⍝ Arithmetic mean.⍞000D⍞000DAn illustration of local definitions.⍞000D⍞000DNB: From Dyalog V14, this function can be coded as the fork:⍞000D⍞000D    mean ← +⌿ ÷ ≢       ⍝ mean along first axis⍞000D⍞000DExample:⍞000D⍞000D    mean 1 2 3 4⍞000D2.5⍞000D    square ← *∘2⍞000D    sqrt ← *∘0.5⍞000D    stdev ← {sqrt(mean square ⍵)-square mean ⍵}     ⍝ standard deviation.⍞000D⍞000D    ⎕pp←5⍞000D⍞000D    stdev 2 2 2⍞000D0⍞000D    stdev 1 2 3⍞000D0.8165⍞000D⍞000D    stdev ← sqrt(mean∘square - square∘mean)         ⍝ ditto using train⍞000D⍞000D⍝ Also:⍞000D⍞000D    mean ← +.÷∘⍴⍨           ⍝ (P.Last)⍞000D    mean ← {1÷.⊥⍵(⍵=⍵)}     ⍝ (G.Streeter Dyalog''14)⍞000D    mean ← {⍵⌹⍵=⍵}          ⍝ (T.Seppälä)⍞000D    mean ← {⌹/⍵∘*¨1 0}      ⍝ (R.Hui)⍞000D    mean ← ⌹∘(=⍨)⍨          ⍝ (R.Hui)⍞000D    mean ← ⊢ ⌹ =⍨           ⍝ same idea using fork⍞000D⍞000DIndex:local names|standard deviation|APL''88⍞000DIndex;Last P.|Streeter G.|Hui R.K.W.|Seppälä T.' 
