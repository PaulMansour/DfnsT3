'i ← bits ##.int u                           ⍝ Signed from unsigned integers.⍞000Du ← bits ##.uns i                           ⍝ Unsigned from signed integers.⍞000D⍞000DThese  little  functions  "cast"  between  signed and unsigned integers. This is⍞000Dsometimes useful when dealing with native files. Note that as all of the primit-⍞000Dive  functions  used  in  the  coding are pervasive, they-just-work on arrays of⍞000Dhigher rank and depth.⍞000D⍞000DTechnical notes:⍞000D⍞000DFor 8-bit (type 83) integers, the functions may be simplified to:⍞000D⍞000D    uns←{256|⍵}                             ⍝ unsigned from signed byte.⍞000D⍞000D    int←{¯128+256|128+⍵}                    ⍝ signed from unsigned byte.⍞000D⍞000Dand for 16-bit short (type 163) integers, to:⍞000D⍞000D    uns←{65536|⍵}                           ⍝ unsigned from 16-bit signed.⍞000D⍞000D    int←{¯32768+65536|32768+⍵}              ⍝ signed from 16-bit unsigned.⍞000D⍞000DThus, in general we have:⍞000D⍞000D    uns←{(2*⍺)|⍵}                           ⍝ unsigned from ⍺-bit signed.⍞000D⍞000D    int←{↑⍵{(⍺|⍵+⍺⍺)-⍵}/2*⍺-0 1}            ⍝ signed from ⍺-bit unsigned.⍞000D⍞000DExamples:⍞000D⍞000D    8 uns ¯3 to 3                           ⍝ 8-bit unsigned.⍞000D253 254 255 0 1 2 3⍞000D⍞000D    16 uns ¯3 to 3                          ⍝ 16-bit unsigned.⍞000D65533 65534 65535 0 1 2 3⍞000D⍞000D    12 uns ¯3 to 3                          ⍝ 12-bit unsigned.⍞000D4093 4094 4095 0 1 2 3⍞000D⍞000D    8 int 8 uns ¯3 to 3                     ⍝ 8-bit round trip.⍞000D¯3 ¯2 ¯1 0 1 2 3⍞000D⍞000D    16 int 16 uns ¯3 to 3                   ⍝ 16-bit round trip.⍞000D¯3 ¯2 ¯1 0 1 2 3⍞000D⍞000D    12 int 12 uns ¯3 to 3                   ⍝ 12-bit round trip.⍞000D¯3 ¯2 ¯1 0 1 2 3⍞000D⍞000D    4 hex 16 uns ¯3 to 3                    ⍝ hex of uns ...⍞000D fffd  fffe  ffff  0000  0001  0002  0003⍞000D    4 hex ¯3 to 3                           ⍝ ... same as hex of int.⍞000D fffd  fffe  ffff  0000  0001  0002  0003⍞000D⍞000D    8 uns 8 int  2 3 4⍴256-⍳24              ⍝ ok for higher rank arrays.⍞000D255 254 253 252⍞000D251 250 249 248⍞000D247 246 245 244⍞000D⍞000D243 242 241 240⍞000D239 238 237 236⍞000D235 234 233 232⍞000D⍞000D    8 int 8 uns ↑,∘⊂/-⍳5                    ⍝ ok for nested arrays.⍞000D┌──┬───────────────┐⍞000D│¯1│┌──┬──────────┐│⍞000D│  ││¯2│┌──┬─────┐││⍞000D│  ││  ││¯3│¯4 ¯5│││⍞000D│  ││  │└──┴─────┘││⍞000D│  │└──┴──────────┘│⍞000D└──┴───────────────┘⍞000D⍞000DSee also: hex⍞000D⍞000DIndex:type conversion|unsigned|integer|pervasive function|native file|cast⍞000DIndex:array, nested' 
