'cvec ← {maxcols←⎕pw} ##.tree space          ⍝ Display of namespace tree.⍞000D⍞000DThe resulting character vector  represents the structure  of all objects  in the⍞000Dargument space. The optional left argument (default ⎕PW), determines the maximum⍞000Dnumber of columns for the result.  Within sub-spaces, objects of each name-class⍞000Dare collected and prefixed with type code:⍞000D⍞000D    ~ variables⍞000D    ∇ functions⍞000D    ∘ operators⍞000D⍞000DNamespaces of special type (Forms, Menus, etc.), are explicitly annotated.⍞000D⍞000D[tree] is a good candidate for invocation using a programmable function key, see⍞000D→PF_keys←.⍞000D⍞000DTechnical notes:⍞000D⍞000DIn  order to avoid name clashes with definitions local to tree itself, auxiliary⍞000Doperand  function exec←{⍺⍎⍵} is installed _outside_ the main body of code.  When⍞000D⎕NL  and ⍎ are called via [exec], local names inside tree are invisible. This is⍞000Done of the benefits of lexical as opposed to dynamic scope rules.⍞000D⍞000DGiven that any space may contain a ref to any other space, the structure is more⍞000Dproperly  termed a _graph_ than a _tree_. Tree pursues only the immediate child-⍞000Dren  of  each  space. Otherwise, _remote_ references appear as: ref → full_name.⍞000D⍞000DNotice  that refs "hidden" within array variables  are invisible to this version⍞000Dof [tree].⍞000D⍞000DExamples:⍞000D⍞000D      80 tree #⍞000D#⍞000D·   ∇ ambiv ascfile attrib cmat cmpx day det dice disp display dot factorial⍞000D·   ∇ factors fibonacci find fndiff fnrefs gcd justify kt lcase life mean mtrim⍞000D·   ∇ osc pack pmat queens refs root roots setwx sieve squeeze ss ssmat ssword⍞000D·   ∇ stamps subs tfmt timestamp tnest to tokens tokens2 tree tview type ucase⍞000D·   ∇ unwrap up vtrim words wrap wsdiff xtabs⍞000D·   ∘ bsearch else file limit perv pow saw traj until⍞000D·   notes⍞000D·   ·   ~ ambiv ascfile attrib bsearch cmat cmpx contents day det dice disp⍞000D·   ·   ~ display else factorial factors fibonacci file find fndiff fnrefs gcd⍞000D·   ·   ~ justify kt lcase life limit mean mtrim osc pack perv pmat pow queens⍞000D·   ·   ~ refs root roots saw setwx sieve squeeze ss ssmat ssword stamps subs⍞000D·   ·   ~ tfmt timestamp tnest to tokens tokens2 traj tree tview type ucase⍞000D·   ·   ~ until unwrap up vtrim words wrap wsdiff xtabs Line_vectors Namespaces⍞000D·   ·   ~ Trees Workspaces⍞000D⍞000D      tree ⎕SE.cbbot.bandsb1        ⍝ Sub-tree of session space.⍞000D⎕SE.cbbot.bandsb1 [CoolBand]⍞000D·   sb [StatusBar]⍞000D·   ·   caps [StatusField]⍞000D·   ·   hint [StatusField]⍞000D·   ·   ins [StatusField]⍞000D·   ·   ·   ∇ KEY_TOGGLE⍞000D·   ·   mm [StatusField]⍞000D·   ·   mode [StatusField]⍞000D·   ·   ·   ∇ KEY_TOGGLE⍞000D·   ·   num [StatusField]⍞000D·   ·   pause [StatusField]⍞000D⍞000D      tree x                        ⍝ Spaces containing ''remote'' refs.⍞000D#.x⍞000D·   ref → #.x⍞000D·   y⍞000D·   ·   ref → #.x.z⍞000D·   z⍞000D·   ·   ref → #.x.y⍞000D⍞000DSee also: refs Namespaces PF_keys cols⍞000D⍞000DIndex:workspace|name-clash problem' 
