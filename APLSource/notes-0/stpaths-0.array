'pvec ← ##.stpaths tree                      ⍝ Spanning tree paths.⍞000D⍞000DReturns  vector [pvec] of the paths from the root to each vertex of the spanning⍞000D[tree].⍞000D⍞000DIt  is  quicker  to extract single paths using →stpath← but if all paths are re-⍞000Dquired, [stpaths] is quicker than {⍵∘stpath¨⍳⍴⍵}.⍞000D⍞000DExamples:⍞000D⍞000D⍝⍞000D⍝      Graph "a".⍞000D⍝    ┌─────1←────┐   5 vertices: 1 2 3 4 5⍞000D⍝    │     │     │⍞000D⍝    ↓     ↓     │   8 edges:    1→2  1→3⍞000D⍝    2←───→3────→4               2→3⍞000D⍝          ↑     │               3→2  3→4⍞000D⍝          │     ↓               4→1  4→5⍞000D⍝          └─────5               5→3⍞000D⍞000D      a←(2 3)(3)(2 4)(1 5)(3)       ⍝ simple origin-1 graph.⍞000D⍞000D      a span 1⍞000D¯1 1 1 3 4⍞000D⍞000D      stpaths a span 1              ⍝ paths from vertex 1.⍞000D┌─┬───┬───┬─────┬───────┐⍞000D│1│1 2│1 3│1 3 4│1 3 4 5│⍞000D└─┴───┴───┴─────┴───────┘⍞000D⍞000DSee also: stpath stdists span Graphs⍞000D⍞000DIndex:graph|spanning tree|path' 
