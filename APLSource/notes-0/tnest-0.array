'tree ← ##.tnest (depth leaves)              ⍝ Array from TreeView style tree.⍞000D⍞000DThe TreeView style representation of a tree is rendered in nested array form.⍞000D⍞000DTechnical note:⍞000D⍞000DAn alternative coding of this function might be:⍞000D⍞000D    tnest←{⎕ML←0                        ⍝ Array from TreeView style tree.⍞000D        1{                              ⍝ starting with split at 1s.⍞000D            1=⍴⍵:⊃⌽↑⍵                   ⍝ atom: leaf value.⍞000D            (⊂⊃⌽⊃⍵),(⍺+1)∇¨(⍺=⊃¨⍵)⊂⍵    ⍝ tree: node followed by subarrays.⍞000D        }↓⍉↑⍵                           ⍝ vector of depth-node pairs.⍞000D    }⍞000D⍞000D⍞000DExamples, See: Trees⍞000D⍞000DSee also: tfmt tview⍞000D⍞000DIndex:tree|formatting, trees' 
