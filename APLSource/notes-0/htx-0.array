'segs ← tag ##.htx html                      ⍝ Extract html segments.⍞000D⍞000DExtracts [tag]-tagged segments from character array [html].⍞000D⍞000DNB: This function may be coded more simply using system function ⎕XML.⍞000D⍞000DRight argument [html] may be:⍞000D⍞000D    - a character vector, possibly containing linefeed characters, or⍞000D    - a character matrix, or⍞000D    - a vector of character vectors (as delivered by →getfile←).⍞000D⍞000DIf [tag] starts with a ''<'' character, the <begin> and </end> tags are themselves⍞000Dincluded  in  the result, otherwise they are omitted. For aesthetic reasons, the⍞000Dclosing ''>'' may also be included in [tag], but is ignored.⍞000D⍞000DTechnical notes:⍞000D⍞000DThe coding is an example of "programming with functions". Notice that nearly all⍞000Dof the local names refer to functions, rather than to data arrays.⍞000D⍞000DExamples:⍞000D⍞000D    bold←''<b>this</b> and <b>that</b>''⍞000D⍞000D      ''b'' htx bold                  ⍝ extract <bold> text.⍞000D┌────┬────┐⍞000D│this│that│⍞000D└────┴────┘⍞000D⍞000D    ''<b>'' htx bold                  ⍝ .. including tags.⍞000D┌───────────┬───────────┐⍞000D│<b>this</b>│<b>that</b>│⍞000D└───────────┴───────────┘⍞000D⍞000D    htm                             ⍝ character vector (with linefeeds).⍞000D<html>⍞000D  <body>⍞000D    <table>⍞000D      <tr><td>%</td><td>Eye Poke</td><td>Kumquat</td></tr>⍞000D      <tr><td>Guys</td><td>60</td><td>40</td></tr>⍞000D      <tr><td>Dolls</td><td>20</td><td>80</td></tr>⍞000D    </table>⍞000D  </body>⍞000D</html>⍞000D⍞000D    ''table''htx htm                  ⍝ extract table.⍞000D┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐⍞000D│ <tr><td>%</td><td>Eye Poke</td><td>Kumquat</td></tr> <tr><td>Guys</td><td>60</td><td>40</td></tr> <tr><td>Dolls</td><td>20</td><td>80</td></tr> │⍞000D└─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘⍞000D⍞000D    ''<table>''htx htm                ⍝ extract table with tags.⍞000D┌────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐⍞000D│<table> <tr><td>%</td><td>Eye Poke</td><td>Kumquat</td></tr> <tr><td>Guys</td><td>60</td><td>40</td></tr> <tr><td>Dolls</td><td>20</td><td>80</td></tr> </table>│⍞000D└────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘⍞000D⍞000D    ''tr''htx htm                     ⍝ extract table rows.⍞000D┌───────────────────────────────────────────┬───────────────────────────────────┬────────────────────────────────────┐⍞000D│<td>%</td><td>Eye Poke</td><td>Kumquat</td>│<td>Guys</td><td>60</td><td>40</td>│<td>Dolls</td><td>20</td><td>80</td>│⍞000D└───────────────────────────────────────────┴───────────────────────────────────┴────────────────────────────────────┘⍞000D⍞000D    ''td''htx htm                     ⍝ extract table data.⍞000D┌─┬────────┬───────┬────┬──┬──┬─────┬──┬──┐⍞000D│%│Eye Poke│Kumquat│Guys│60│40│Dolls│20│80│⍞000D└─┴────────┴───────┴────┴──┴──┴─────┴──┴──┘⍞000D⍞000D    ↑''td''∘htx¨''tr''htx htm           ⍝ extract table data per row.⍞000D┌─────┬────────┬───────┐⍞000D│%    │Eye Poke│Kumquat│⍞000D├─────┼────────┼───────┤⍞000D│Guys │60      │40     │⍞000D├─────┼────────┼───────┤⍞000D│Dolls│20      │80     │⍞000D└─────┴────────┴───────┘⍞000D⍞000DSee also: Line_vectors html getfile⍞000D⍞000DIndex:XML|tag, XML|Kumquat' 
