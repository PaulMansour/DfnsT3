'enc ← ##.rep name                           ⍝ Encapsulate function/operator.⍞000D⍞000DReturns an encapsulation of the function or operator [name] suitable for storing⍞000Din  an array or on file. Notably, the representation accommodates named, derived⍞000D(or "assigned") functions, such as: sum←+/.⍞000D⍞000DTechnical note:⍞000D⍞000DFor  traditional and D- functions/operators, ⎕or could be used directly, but the⍞000D⎕or of a derived function does not include its name for re-fixing. In this case,⍞000Dthe  capsule is a (name ⎕or) pair. Note that the rank of ⎕cr is used to disting-⍞000Duish:⍞000D⍞000D    0=⍴⍴⎕cr ⍵:  primitive / system function.⍞000D    1=⍴⍴⎕cr ⍵:  derived function.⍞000D    2=⍴⍴⎕cr ⍵:  defined / D-function.⍞000D⍞000DExample:⍞000D⍞000D    ⎕fx''z←tfn'' ''z←⎕ts''          ⍝ trad fn.⍞000D    dfn←{3↑⍵}                   ⍝ D fn.⍞000D    afn←×/                      ⍝ assigned fn.⍞000D⍞000D    rep¨ ''tfn'' ''dfn'' ''afn''⍞000D┌─────────────┬───────────┬────────┐⍞000D│┌─────┬─────┐│┌─────────┐│┌───┬──┐│⍞000D││z←tfn│z←⎕TS│││dfn←{3↑⍵}│││afn│×/││⍞000D│└─────┴─────┘│└─────────┘│└───┴──┘│⍞000D└─────────────┴───────────┴────────┘⍞000D⍞000DSee also: fix mns⍞000D⍞000DIndex:function array|encapsulate function' 
