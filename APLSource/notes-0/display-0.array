'cmat ← ##.display  array                    ⍝ Boxed display of array.⍞000Dcmat ← ##.displays array                    ⍝ .. with axis lengths.⍞000Dcmat ← ##.displayr array                    ⍝ ..    ..  and subarray depths.⍞000D⍞000DThe argument array is  displayed  with boxes bordering each subarray. Characters⍞000Dembedded in the borders indicate subarray shape and type.⍞000D⍞000DShape:  ↓ → Non-zero axis.⍞000D        ⌽ ⊖ Zero axis.⍞000D⍞000DType:     ∊ Nested array.⍞000D          ~ Numeric.⍞000D          - Character.⍞000D          # Namespace reference.⍞000D          ∇ ⎕OR object.⍞000D          + Mixed type.⍞000D⍞000DPrototypical items of empty arrays (⌽ ⊖) are exposed.⍞000D⍞000DWith  one  exception,  [display] perfectly distinguishes arrays. That is to say,⍞000Ddistinct  arrays  have distinct display forms. The exception, signalled by a ''+''⍞000Dcharacter  in  its  left  lower border, is a depth-1 (sub) array containing both⍞000Dcharacters and numbers. For example: display 88,''99''.⍞000D⍞000DVariations⍞000D----------⍞000DInspired  by  David  Rabenhorst''s  1988  paper, Dick Bowman supplies two further⍞000Dversions of the display function⍞000D⍞000D    displays    ⍝ display with shape along top border.⍞000D    displayr    ⍝ display with shape along axis borders and with depth.⍞000D⍞000DCompare the output from Dick''s functions with the original:⍞000D⍞000D        display 1 ''a'' ''abc'' (2 3⍴⍳6)    ⍝ "standard" display.⍞000D    ┌→──────────────────┐⍞000D    │     ┌→──┐ ┌→────┐ │⍞000D    │ 1 a │abc│ ↓1 2 3│ │⍞000D    │   - └───┘ │4 5 6│ │⍞000D    │           └~────┘ │⍞000D    └∊──────────────────┘⍞000D⍞000D        displays 1 ''a'' ''abc'' (2 3⍴⍳6)   ⍝ with shape along top border.⍞000D    ┌→─4────────────────┐⍞000D    │     ┌→─3┐ ┌→─2 3┐ │⍞000D    │ 1 a │abc│ ↓1 2 3│ │⍞000D    │   - └───┘ │4 5 6│ │⍞000D    │           └~────┘ │⍞000D    └∊──────────────────┘⍞000D⍞000D        displayr 1 ''a'' ''abc'' (2 3⍴⍳6)   ⍝ with shape per axis and depth.⍞000D    ┌4────────────────────┐⍞000D    │     ┌3───┐ ┌3─────┐ │⍞000D    │ 1 a │ abc│ 2 1 2 3│ │⍞000D    │   - └────┘ │ 4 5 6│ │⍞000D    │            └~─────┘ │⍞000D    └¯2───────────────────┘⍞000D⍞000DRef: "The Compact Display of Arbitrary Nested Arrays" D. A. Rabenhorst (IBM),⍞000D     APL88 Conference Proceedings, pp 272-277, ACM - 0-89791-253-5/88/0002/0272⍞000D⍞000DExamples:⍞000D⍞000D      display ''ABC''(1 4⍴1 2 3 4)(0 1 0⍴0)(⎕SE #)(''88'',99)⍞000D┌→────────────────────────────────────────┐⍞000D│ ┌→──┐ ┌→──────┐ ┌┌⊖┐ ┌→───────┐ ┌→────┐ │⍞000D│ │ABC│ ↓1 2 3 4│ ⌽↓0│ │ ⎕SE  # │ │88 99│ │⍞000D│ └───┘ └~──────┘ └└~┘ └#───────┘ └+────┘ │⍞000D└∊────────────────────────────────────────┘⍞000D⍞000DSee also: disp box dsp⍞000D⍞000DIndex:display (of arrays)|box|boxing|box-drawing characters⍞000DIndex;Rabenhorst D.A.|Bowman D.' 
