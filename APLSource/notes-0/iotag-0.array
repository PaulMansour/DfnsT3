'rslt ← {array} ##.iotag array               ⍝ Generalized iota.⍞000D⍞000DSupplied by Steve Mansour:⍞000D⍞000DMonadic form produces generalized Index Generator function.⍞000DDyadic form with simple scalar left argument produces interval function.⍞000DDyadic form with rank 1 or higher left argument produces generalized Index Of⍞000Dfunction.⍞000D⍞000DThe following table describes the function in detail:⍞000D⍞000D┌──────────────────┬──────────────────┬────────────────────────────────────────┐⍞000D│ Left Argument    │ Right Argument   │  Function/Result                       │⍞000D├──────────────────┼──────────────────┼────────────────────────────────────────┤⍞000D│                  │ Scalar integer   │  Index Generator:   iotag ⍵ ←→ (×⍵)×⍳|⍵│⍞000D│                  │ ⎕IO←0, ⍵≥0       │  0 1 2 ... ⍵-1                         │⍞000D│                  │ ⎕IO←0, ⍵<0       │  0 ¯1 ¯2 ... ⍵+1                       │⍞000D│ None (monadic)   │ ⎕IO←1, ⍵≥0       │  1 2 3 ... ⍵                           │⍞000D│                  │ ⎕IO←1, ⍵<0       │  ¯1 ¯2 ¯3 ... ⍵                        │⍞000D│                  ├──────────────────┼────────────────────────────────────────┤⍞000D│                  │ Character scalar │  Alphabetic Index (⎕IO independent)    │⍞000D│                  │ ⍵∊''ABC...Z''      │  ''ABC...⍵''    iotag ''Z'' ←→ ⎕A          │⍞000D│                  │ ⍵∊''abc...z''      │  ''abc...⍵''    iotag ''d'' ←→ ''abcd''      │⍞000D│                  │ ⍵∊''012345789''    │  ''012...⍵''    iotag ''9'' ←→ ⎕D          │⍞000D│                  │ ⍵='' ''            │  ''''                                    │⍞000D├──────────────────┼──────────────────┼────────────────────────────────────────┤⍞000D│ Scalar Integer   │ Scalar integer   │ Interval (⎕IO independent)             │⍞000D│ (0=⍴⍴⍺)          │ ⍺≤⍵              │ ⍺ (⍺+1) (⍺+2) ... ⍵                    │⍞000D│                  │ ⍺>⍵              │ ⍺ (⍺-1) (⍺-2) ... ⍵                    │⍞000D│                  ├──────────────────┼────────────────────────────────────────┤⍞000D│                  │ 2-Item vector    │ Step Interval (⎕IO independent)        │⍞000D│                  │ (z s)←⍵          │ Endpoint, [Stepsize]                   │⍞000D│                  │ ⍺≤z              │ ⍺ (⍺+s) (⍺+s×2) ... (z⌊⍺+s×n)          │⍞000D│                  │ ⍺>z              │ ⍺ (⍺-s) (⍺-s×2) ... (z⌈⍺-s×n)          │⍞000D├──────────────────┼──────────────────┼────────────────────────────────────────┤⍞000D│ Character scalar │ Character scalar │ Character Interval (⎕IO independent)   │⍞000D│ (0=⍴⍴⍺)          │                  │ ⍺ iotag ⍵ ←→ ⎕AV[↑iotag/⎕AV⍳⍺ ⍵]       │⍞000D│                  │                  │ ''B'' iotag ''F'' ←→  ''BCDEF''              │⍞000D│                  │                  │ ''z'' iotag ''w'' ←→  ''zyxw''               │⍞000D├──────────────────┼──────────────────┼────────────────────────────────────────┤⍞000D│ Vector (1=⍴⍴⍺)   │ Any array        │ Index of (traditional dyadic ⍳)        │⍞000D├──────────────────┼──────────────────┼────────────────────────────────────────┤⍞000D│ Matrix (2=⍴⍴⍺)   │ Vector           │ Generalized Index of                   │⍞000D│                  │ (1=⍴⍴⍵)          │ Index of row (ignore trailing blanks)  │⍞000D│                  ├──────────────────┼────────────────────────────────────────┤⍞000D│                  │ Matrix (2=⍴⍴⍵)   │ Index of each row                      │⍞000D│                  ├──────────────────┼────────────────────────────────────────┤⍞000D│                  │ Array (3≤⍴⍴⍵)    │ Index of each row                      │⍞000D├──────────────────┼──────────────────┼────────────────────────────────────────┤⍞000D│ Array  (3≤⍴⍴⍺)   │ Matrix (2=⍴⍴⍵)   │ Index of (hyper)plane                  │⍞000D│                  ├──────────────────┼────────────────────────────────────────┤⍞000D│                  │ Array  (3≤⍴⍴⍵)   │ Index of each (hyper)plane             │⍞000D└──────────────────┴──────────────────┴────────────────────────────────────────┘⍞000D⍞000DNote that inner function "ischar" accepts Unicode characters from Dyalog 11.1.⍞000D⍞000DExamples:⍞000D⍞000D    Beatles APLers Names⍞000D┌──────┬─────┬──────┐⍞000D│JOHN  │STEVE│PAUL  │⍞000D│PAUL  │PAUL │STEVE │⍞000D│GEORGE│JOHN │BILL  │⍞000D│RINGO │PETE │GEORGE│⍞000D└──────┴─────┴──────┘⍞000D⍞000D    APLers iotag ''STEVE''        ⍝ Find row of matrix⍞000D0⍞000D    ⍴APLers iotag 1 5⍴''STEVE''   ⍝ One-row matrix results in 1-item vector result⍞000D1⍞000D    Beatles iotag ''GEORGE    ''  ⍝ Ignores trailing blanks (and trailing 0''s)⍞000D2⍞000D    APLers iotag Beatles        ⍝ Find multiple rows of matrix⍞000D2 1 4 4⍞000D    ⍴Rank3←⊃APLers Beatles      ⍝ Rank 3 array⍞000D2 4 6⍞000D    Rank3 iotag Beatles         ⍝ Find matrix in Rank-3 array⍞000D1⍞000D    Beatles iotag Rank3         ⍝ Find Vectors in matrix⍞000D4 1 0 4⍞000D0 1 2 3⍞000D⍞000D    3 iotag 5 .5                ⍝ Step of 0.5⍞000D3 3.5 4 4.5 5⍞000D⍞000D    12 iotag 3 3                ⍝ Step of 3⍞000D12 9 6 3⍞000D⍞000DIndex:iota, generialised|⍳, generalised|Unicode⍞000DIndex;Mansour S.M.' 
