'lines ← {tabs←0 {ctype←80}} ##.getfile filename  ⍝ Text file ⍵ as nested vector.⍞000D⍞000DReturns  text  file  as a vector of character vectors using default ASCII to ⎕AV⍞000D(or  current  ⎕nxlate 0 mapping if this has been changed), translation. The file⍞000Dcontent  is  split  at [LF] chars, and [CR] chars are removed.⍞000D⍞000DOptional  left  argument {tabs} specifies tabstop positions for tab expansion. A⍞000Dvalue  greater than 0 (the default) causes horizontal tab characters in the file⍞000Dto be replaced with an appropriate number of blanks in the result.⍞000D⍞000DOptional  left argument {ctype} specifies the type of characters to be read from⍞000Dthe  file. In pre-unicode versions of the interpreter, this would always be left⍞000Dto default or set to 82. In unicode interpreters, the default is 80 but it could⍞000Dbe set to 160 or 320.⍞000D⍞000DNote  that tabs can be expanded after reading the file, using →xtabs←¨, but this⍞000Dis not quite as fast.⍞000D⍞000DExample:⍞000D⍞000D      {⎕←⍵}¨12↑8 getfile''c:/dyalog90/aplfmt/default.dft''⍞000D+-----------------------------------------------+⍞000D+            D a t e   F o r m a t s            +⍞000D+-----------------------------------------------+⍞000D⍞000DD:Sunday⍞000DD:Monday Tuesday Wednesday Thursday Friday⍞000DD:Saturday⍞000D⍞000DM:January       February        March⍞000DM:April         May             June⍞000DM:July          August          September⍞000DM:October       November        December⍞000D⍞000DSee also: putfile xtabs⍞000D⍞000DIndex:native file|text file|Unicode' 
