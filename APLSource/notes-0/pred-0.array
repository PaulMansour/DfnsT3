'rslt ← part (redn ##.pred) rarg             ⍝ Partitioned reduction.⍞000D⍞000DSuggested  by Michael Baas, pred returns a partitioned reduction of its argument⍞000Darray. [part] is a vector of partition sizes, such that (+/part)=¯1↑⍴rarg.⍞000D⍞000DWays to code this operator include:⍞000D⍞000D    pred←{⎕ml←1 ⋄ ⍺⍺/¨(∊⍺↑¨1)⊂⍵}                    ⍝ --3   SL⍞000D    pred←{(⎕io ⎕ml)←1 3 ⋄ ⍺⍺/¨(⍺/⍳⍴⍺)⊂⍵}            ⍝ 1-3   VMJ/MJ⍞000D    pred←{(⎕io ⎕ml)←1 3 ⋄ ⍺⍺/⊃(⍺/⍳⍴⍺)⊂⍵}            ⍝ 12-   GQ⍞000D    pred←{res←(⍴⍺)⍴⍺⍺/⍬ ⋄ res[⍺/⍳⊃⍴⍺]⍺⍺←⍵ ⋄ res}    ⍝ -2-   BT⍞000D⍞000D    pred←{⎕ml←3 ⋄  ⍺⍺ nz/⊃(⍺/⍳⍴⍺)⊂⍵}                ⍝ 12-   SM⍞000D    pred←{⎕io←0 ⋄ (<⍀(+\⍺)∘.>⍳⍴⍵)⍺⍺ nz.×⍵}          ⍝ -2-   SM⍞000D    pred←{⎕io←0 ⋄ ⍉(<⍀(+\⍺)∘.>⍳¯1↑⍴⍵)⍺⍺ nz.×⍉⍵}     ⍝ 12-   SM, where:⍞000D      nz←{0≠⍺×⍵: ⍺ ⍺⍺ ⍵ ⋄ ⍺=0:⍵ ⋄ ⍺}                ⍝ auxiliary op.⍞000D⍞000DNote:⍞000D⍞000D1. Works for matrix (and higher rank) rarg.⍞000D2. "Each-less" solution.⍞000D3. Unrestricted operand function (eg: '','').⍞000D⍞000DExamples:⍞000D⍞000D    2 3 3 2 +pred ⍳10⍞000D3 12 21 19⍞000D⍞000D    1 2 1 ×pred 2 3 4⍴⍳24⍞000D 1   6  4⍞000D 5  42  8⍞000D 9 110 12⍞000D⍞000D13 210 16⍞000D17 342 20⍞000D21 506 24⍞000D⍞000D    1 2 1,pred 3 4⍴⍳12⍞000D┌─┬─────┬──┐⍞000D│1│2 3  │4 │⍞000D├─┼─────┼──┤⍞000D│5│6 7  │8 │⍞000D├─┼─────┼──┤⍞000D│9│10 11│12│⍞000D└─┴─────┴──┘⍞000D⍞000DSee also: acc foldl trav⍞000D⍞000DIndex:partioned reduction⍞000DIndex;Baas M.' 
