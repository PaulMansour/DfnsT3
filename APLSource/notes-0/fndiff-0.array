'cmat ← {excl} ##.fndiff (fna fnb)           ⍝ Defined function differences.⍞000D⍞000DFndiff  takes a pair of function names of the form ''fnid'' or ''wsid fnid''. Where,⍞000D[fnid]  is a plain or space-referenced function or operator name and [wsid] is a⍞000Dworkspace id.⍞000D⍞000DIf  [wsid]  is given and a directory path is not supplied, WSPATH (See Options->⍞000DConfigure->Workspace) is used to locate the saved workspace.⍞000D⍞000DOtherwise, the function is sought in the current active workspace. The result is⍞000Da  "split-screen"  display  of lines that are in one function but not the other.⍞000DThe optional left argument is a character vector exclusion list:⍞000D⍞000D    '' '' Exclude white space from the comparison.⍞000D    ''⍝'' Exclude comments from the comparison.⍞000D⍞000DIn order to accommodate [wsids] containing embedded blanks, the _last_ word in a⍞000Dfunction  specification  is taken as the [fnid] and everything to the left of it⍞000D(omitting leading and trailing blanks) is taken as the [wsid]. Embedded multiple⍞000Dblanks are preserved:⍞000D⍞000D    ''  My Documents\WS  myfn '' => wsid ≡ ''My Documents\WS''   fnid ≡ ''myfn''⍞000D⍞000DExamples:⍞000D⍞000D      ''⍝''fndiff''min trees'' ''max trees''⍞000D·   0=≡⍵:⍵  ·   ·   ·   ·   ·   ·   · │·   0=≡⍵:⍕⍵ ·   ·   ·   ·   ·   ·   ·⍞000D·   ~(⊂⊃⍵)∊''@\='':⍺∘∇¨⍵  ·   ·   ·   · │·   (0≡⊃0⍴⍵)∨(0≠≡⊃⍵)∨(,3)≢⍴⍵:⍺∘∇¨⍵  ·⍞000D⍞000D      ''⍝''fndiff''display DISPLAY'' ''display''⍞000DDISPLAY←{⎕IO·⎕ML←0  ·   ·   ·   ·   · │display←{⎕IO·⎕ML←0  ·   ·   ·   ·   ·⍞000D·   ⍺←0 ⋄ chars←⍺⊃''..''''''''|-''·''┌┐└┘│─'' │·   ⍺←1 ⋄ chars←⍺⊃''..''''''''|-''·''┌┐└┘│─''⍞000D⍞000DSee also: wsdiff Workspaces⍞000D⍞000DIndex:difference, functions|compare functions' 
